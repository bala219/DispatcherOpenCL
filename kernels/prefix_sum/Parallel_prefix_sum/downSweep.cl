__kernel void upSweep(__global const unsigned int *_sorted_element,
                        __global unsigned int *_index_arr,
                        __global unsigned int *_offset_arr) {


    for(size_t level = 0; level < DATA_SIZE/2;level++){
        //For each level some value has to be processed
    }

}